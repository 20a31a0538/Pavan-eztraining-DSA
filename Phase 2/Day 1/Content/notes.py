#lamda funtion--->Anonymous function
#without function name ---> where we apply concept of lamba
l=[1,2,3]
r=(map(lambda x:x+x,l))
print(list(r))

# finding powers using lambda
l1=[4,5,6]
res=(map(lambda n:pow(n,2),l1))
print(list(res))

#string
name ="pavan"
(lambda n1:print(name))(name)  #print() used
#print(res1)

print(\n)

#four pillars of oops
# 1. encapsulation 2.abtraction 3. inheritance 4. polymorphism
#abstraction --> hiding the implementation part , showing on lywhat is requied
# example :exe file
#2.encapsulation
#we can make class or method as abstract , opposite to abstract is concrete

print(\n)
#from module(like math)import particular_methods(;ike sqrt)-----> increase efficient
print(\n)
#Encapsulation --->3 types : public , private , protected

#Inheritance ---> based_class : parent
#                 derived class : child
# derived class will inherit properies from based_class
#types of inheritance :
#1. single #2. multiple 3. multi_level 4. Hierarical 5. Hybrid

#protected --> 
